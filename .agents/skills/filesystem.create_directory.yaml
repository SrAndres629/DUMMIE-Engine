skill_id: sw.fs.create_directory
name: Create Directory
description: Create a new directory or ensure a directory exists. Can create multiple
  nested directories in one operation. If the directory already exists, this operation
  will succeed silently. Perfect for setting up directory structures for projects
  or ensuring required paths exist. Only works within allowed directories.. Gestión
  industrial de archivos.. Gestión industrial de archivos.. Gestión industrial de
  archivos.
version: 1.1.0
capabilities:
- create_directory
invariants:
- 'path_validation: strict'
- 'size_limit: 1MB'
- 'zero_trust: true'
metadata:
  server: filesystem
  status: active
