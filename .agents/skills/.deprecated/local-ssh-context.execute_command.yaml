skill_id: sw.local-ssh-context.execute_command
name: Execute Command
description: Execute shell command on remote server. Utilidad del servidor local-ssh-context..
  Utilidad del servidor local-ssh-context.. Utilidad del servidor local-ssh-context.
version: 1.1.0
capabilities:
- execute_command
invariants:
- 'compliance: standard'
metadata:
  server: local-ssh-context
  status: active
