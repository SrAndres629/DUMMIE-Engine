skill_id: sw.everything.get-env
name: Get-Env
description: Returns all environment variables, helpful for debugging MCP server configuration.
  Utilidad del servidor everything.. Utilidad del servidor everything.. Utilidad del
  servidor everything.
version: 1.1.0
capabilities:
- get-env
invariants:
- 'compliance: standard'
metadata:
  server: everything
  status: active
