skill_id: sw.local-ssh-context.sftp_upload
name: Sftp Upload
description: Upload content to a file on the remote server via SFTP. Utilidad del
  servidor local-ssh-context.. Utilidad del servidor local-ssh-context.. Utilidad
  del servidor local-ssh-context.
version: 1.1.0
capabilities:
- sftp_upload
invariants:
- 'compliance: standard'
metadata:
  server: local-ssh-context
  status: active
