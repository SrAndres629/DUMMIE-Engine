{
  "spec_id": "176_daemon_gateway_heartbeat_bridge",
  "title": "Daemon/Gateway Heartbeat Bridge Rules",
  "invariants": {
    "supported_targets": ["local", "daemon_invocation", "gateway", "antigravity", "codex", "gemini", "human_review"],
    "supported_modes": ["observe_only", "advisory", "repair_planning", "patch_proposal"]
  },
  "gates": {
    "human_gate": "If requires_human_approval is true, can_execute_now MUST NOT be true.",
    "mutation_gate": "Any action mutating files or systems must have requires_human_approval set to true."
  }
}
