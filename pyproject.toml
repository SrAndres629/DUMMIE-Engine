[project]
name = "dummie"
version = "0.1.0"
description = "DUMMIE Sovereign CLI & SDK"
requires-python = ">=3.11"
dependencies = [
    "ollama>=0.6.2",
    "pyyaml>=6.0.0",
]

[project.scripts]
dummie = "dummie.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
