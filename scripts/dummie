#!/usr/bin/env bash
# Execute dummie CLI using the local venv Python
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="$ROOT_DIR:$PYTHONPATH"
exec "$ROOT_DIR/.venv/bin/python3" -m dummie "$@"
