# Memoria Operativa: 20 de Mayo de 2026

## Hitos de Evolución Soberana
El DUMMIE Engine ha transicionado de un framework pasivo (Read-Only) a un **Hipervisor de Agentes Soberanos (Agent Mesh)**.

### 1. Desmantelamiento del Simulacro (Escritura Soberana)
- **Problema:** El sistema estaba atrapado en simulaciones (`dry_run`). L3 vetaba ejecuciones reales, L5 simulaba comandos de shell, y L2 no persistía la memoria causal en la base de grafos (KuzuDB).
- **Resolución:** Elevamos la autoridad. Modificamos `AuthorityGate` para habilitar `SVRN_CONFIRM` a los roles `OVERSEER` y `ARCHITECT`. `WorkstationOperator` ahora ejecuta en el host local bajo una zona segura. `KuzuGraphSyncAdapter` ahora aplica escrituras físicas por defecto en KuzuDB, cristalizando el grafo 4D-TES real.

### 2. Arnés Multi-Agente (MAD Mesh)
- **Problema:** DUMMIE era visto como un CLI más, cuando en realidad es el orquestador maestro. Los CLIs estaban aislados.
- **Resolución:** Implementé `CliHarness` en L0 (Go). Este arnés envuelve procesos de CLI (Gemini, Codex) y les inyecta un `AgentContext` nativo (prompts, skills, autoridad). Establece una topología de red P2P (vía NATS) con 2 canales de entrada (Overseer Cmd, Peer Msg) y 2 de salida (Status, Peer Broadcast).

## Lecciones para el Futuro
- La verdadera soberanía requiere que las reglas de orquestación y metacognición estén *en el motor*, no en el prompt de la sesión. 
- Necesitamos automatizar la "cristalización" de memoria para que, cuando el CLI termine un comando, DUMMIE capture automáticamente el porqué y actualice el `.aiwg`. Esto se abordará implementando hooks obligatorios en el ciclo de vida del agente.

---

## Cirugía Estructural (Sesión 2026-05-20 Tarde)

### Bugs Reales que Ocultó el Redirector
1. **Indentación en daemon.py:** `GatewayRequest`, `ModelTier`, `BaseAuditor`, `FailClosedAuditor` indentados dentro de `if __package__ in {None, ""}:`. Nunca se ejecutaban. ~2 meses oculto.
2. **Split-class DiagnosticReporter:** daemon importaba `daemon_diagnostic` (→ flat_brain) mientras canónico en `daemon/daemon_diagnostic.py`. `isinstance()` daba False.
3. **FlatBrainFallbackFinder prioridad:** Módulos anidados (p.ej. `metacognition.contracts`) siempre resolvían a flat_brain aunque existiera versión canónica.

### Lo que se Construyó
- `scripts/migrate_flat_to_canonical.py`: CLI de migración flat→canónico
- `scripts/auto_health.py`: Monitor de salud con pytest in-process
- `scripts/dummie-health/main.go`: CLI Go poliglota
- `infrastructure/knowledge_ports.py`: `KnowledgeWriteBridge` (escritura Obsidian + .aiwg/)
- ToolNeedDetectorHook: De etiquetador pasivo a bloqueador activo de acciones externas

### Identidad y Metacognición
- `.aiwg/identity/dummie_identity.yaml` expandido con personalidad, voz, metacognición, blindspots, roadmap evolutivo
- `.aiwg/identity/goal_memory.yaml` limpiado (entradas corruptas con system directives removidas)
- `MEMORY.md` actualizado con ADR 2026-05-20-GOV-005 y GOV-006
- Cron de auto_health instalado (cada hora)

### Estado Final
- 153 tests core pasan, 0 fallos en daemon/metacognition/causal gates/authority
- Go tool build OK
- AuthorityLevel SSoT: 1 archivo, 0 duplicados
- 120 módulos flat_brain pendientes (fallback transparente activo)
- Branch: `intento-de-registrar-todo-con-arquitectura-y-estructura-canon`

### Pendiente Estratégico
Reemplazar heuristicas YAML de identidad con **embeddings semánticos reales** en 4D-TES. Implementar **modelos locales agenticos de razonamiento** (tipo llama.cpp con fine-tuning) para deliberación sin dependencia de API externa. Modelos especializados por clase de acción: social, code, business, admin.