# 2026-05-19

## Provider Activation & OpenCode Integration

### What was done
1. **OpenRouter activated**: Key from user added to ~/.zshrc as `OPENROUTER_API_KEY`, `DUMMIE_OPENROUTER_API_KEY`, `DUMMIE_CLOUD_PREM_API_KEY`, `DUMMIE_CLOUD_PREM_BASE_URL=https://openrouter.ai/api/v1`
2. **Groq activated**: Key from user added to ~/.zshrc as `GROQ_API_KEY`, `DUMMIE_GROQ_API_KEY`, `DUMMIE_CLOUD_STD_API_KEY`, `DUMMIE_CLOUD_STD_BASE_URL=https://api.groq.com/openai/v1`
3. **OpenCode ↔ DUMMIE bidirectional bridge**:
   - OpenCode → DUMMIE: Added MCP server `dummie-brain` in `~/.config/opencode/opencode.jsonc` pointing to `layers/l1_nervous/mcp_server.py`
   - OpenCode → OpenRouter: Added `provider.openrouter` config in opencode.jsonc
   - DUMMIE → OpenCode: Fixed `_opencode_has_credentials()` in `dummie/providers.py` to handle provider-keyed auth.json format; copied auth to DUMMIE-managed path
4. **~/.aider/oauth-keys.env** updated with both new keys

### Provider status (all ready)
- openrouter ✅ (cloud prem tier via DUMMIE_CLOUD_PREM_*)
- groq ✅ (cloud std tier via DUMMIE_CLOUD_STD_*)
- opencode ✅ (local CLI + model routing)
- gemini_cli ✅
- codex_cli ✅

### Files changed
- ~/.zshrc — added env vars for all API keys
- ~/.aider/oauth-keys.env — updated with new keys
- ~/.config/opencode/opencode.jsonc — added dummie-brain MCP + openrouter provider
- dummie/providers.py — fixed _opencode_has_credentials() for provider-keyed auth.json

### Notes
- User also shared an OpenRouter skill URL for building agents with @openrouter/sdk (Node.js/TS). Not integrated — DUMMIE uses Python/urllib/httpx for OpenAI-compatible calls. The env vars set here enable that pathway.
- OpenCode restart required for MCP config to take effect.

## Deep DUMMIE-OpenCode Integration (continuation)

### What was built
1. **OpenCode agent** `.opencode/agent/dummie-engine.md` — subagent mode, auto-discoverable via `@dummie-engine`. Contains:
   - Full architecture overview of all DUMMIE subsystems
   - Meta-gateway protocol (discover → analyze → execute)
   - Quick-start patterns for memory, embeddings, routing
   - Discovery flow for unknown tool scenarios

2. **OpenCode skill** `.opencode/skills/openrouter-agent/SKILL.md` — skill for building agents with @openrouter/sdk (Node.js/TypeScript).

3. **opencode.jsonc enhanced** — added `instructions` (AGENTS.md, MEMORY.md), `skills.paths` (.agents/skills), `mcp.dummie-brain.timeout=120s`

### Integration architecture
```
OpenCode CLI
├── @dummie-engine agent (subagent, knows DUMMIE's full protocol)
├── MCP → dummie-brain (layers/l1_nervous/mcp_server.py)
│   ├── dummie_discover_capabilities()  — semantic tool search
│   ├── dummie_analyze_capability()     — JSON schema per tool
│   └── dummie_execute_capability()     — execute ANY of 80+ tools
│       ├── local.crystallize           — 4D-TES memory persist
│       ├── local.semantic_recall       — semantic memory recall
│       ├── local.reasoned_rerank       — local reasoning rerank
│       ├── local.context_shaper        — context optimization
│       └── 80+ more internal tools
├── skills → openrouter-agent, .agents/skills/*, mcp_gateway
└── providers → openrouter (via apiKey config)
```

### Files created/modified (in repo)
- `.opencode/agent/dummie-engine.md` (NEW)
- `.opencode/skills/openrouter-agent/SKILL.md` (NEW)
- `~/.config/opencode/opencode.jsonc` (enhanced)

### Status
- Provider activation: ✅ OpenRouter, ✅ Groq, ✅ OpenCode, ✅ Gemini CLI, ✅ Codex CLI
- Bidirectional bridge: ✅ OpenCode → DUMMIE (MCP), ✅ DUMMIE → OpenCode (provider #1 priority)
- Agent integration: ✅ @dummie-engine subagent with meta-gateway protocol
- Skill integration: ✅ openrouter-agent skill
- Full memory/embedding/routing access: ✅ via meta-gateway

### Corrections after audit
- Tool count: 42 (not 80+) — 6 public MCP + 36 internal tools across 8 domains
- Removed hardcoded API key from opencode.jsonc (uses env var now)
- Fixed agent file with accurate tool inventory table
- `.opencode/` already had `@opencode-ai/plugin` installed (npm dependency) — may want a proper plugin in future
- `.opencode/.gitignore` ignores node_modules/package.json/package-lock.json

### Actual tool domains (36 internal)
1. Core (5): calibrate_neural_links, metacognitive_status, brain_ping, operational_truth_report, read_spec
2. Memory/Nervous (8): crystallize, log_lesson, resolve_ambiguity, sync_cognitive_state, compress_context, quantize_context, ssh_grep, yield_and_notify
3. Reasoning (5): semantic_recall, tool_card_resolver, reasoned_rerank, context_shaper, selection_feedback
4. Knowledge (6): knowledge_search_context, knowledge_get_artifact, knowledge_ingest_artifact, knowledge_export_decision_summary, knowledge_export_lesson, knowledge_export_session_summary
5. Swarm (4): broadcast_intent, observe_swarm, delegate_task, spawn_agent
6. SDD (3): sdd_evaluate_change_admission, sdd_generate_golden_path, sdd_evaluate_runtime_guard
7. Self-Worktree (3): dummie_self_session_start, dummie_self_session_status, dummie_self_plan_next_loop
8. Metacognition (2): dummie_metacognitive_analyze, dummie_authority_check

## Deletion Incident Audit + PACK R2.1 Repair

### What happened
- User requested full deletion audit of repo after flat_brain/ migration
- Audit confirmed: 0 local deletions, 3371 committed deletions, 58 broken specs, 5 files needing restore
- Decision: FAIL — unsafe migration without manifest

### What was done
1. **Deletion Incident Audit** (commit 2025896)
   - Created backup branch `backup/before-deletion-audit-20260519-065706`
   - 20 audit reports in `.aiwg/reports/`
   - Identified 5 files for restore, 58 broken specs

2. **PACK R2.1 Repair** (commit e6240e9)
   - Restored 5 files: lessons.jsonl, mcp_server.py, test_mcp_server.py, ports.py, schemas.py
   - Repaired 58 broken spec references (50 files, 59 path replacements)
   - Created retroactive migration manifest (235 files mapped)
   - Spec validation: FAIL → PASS (83 specs OK)
   - Decision: PASS_WITH_WARNINGS

3. **Strategic Thinking System** (commit 205e5bf)
   - Created `.aiwg/strategic/` directory
   - `l2_brain_architecture_thinking.md` — honest analysis of L2 state and "48 hours" reality
   - `l2_brain_execution_plan.json` — 6-session distributed plan for PACK R3
   - `dummie_runtime_profile.yaml` — brutally honest self-assessment of capabilities
   - `l2_canonical_manifest.yaml` — proposed canonical L2 architecture
   - `README.md` — index and continuity protocol

### Key decisions
- Branch `intento-de-registrar-todo-con-arquitectura-y-estructura-canon` is forensic audit branch — do NOT merge to main yet
- PACK R3 requires Jorge's input on 5 architectural decisions before execution
- DUMMIE operates in sessions (not 24/7), with file-based continuity
- "48 hours" = 6 sessions of 2-3h each over 2-3 days, not continuous operation

### Pending Jorge input
1. flat_brain/ permanence (keep or reorganize?)
2. src/brain/ role (absorb or eliminate?)
3. Test location (inside flat_brain/ or separate?)
4. Import strategy (compatibility shim or full migration?)
5. Persistence policy (KùzuDB only or KùzuDB + Redb?)

## Autonomous Operation Redesign (CORRECTION)

### What changed
- Jorge corrected my conservative assessment: "la idea es que podas operar en todo momento"
- Previous profile described current limitations, not design objective
- Redesigned DUMMIE as 24/7 event-driven autonomous strategic partner

### New design (commit 03c22b5)
- **Event-driven operation**: git hooks, file watchers, cron, external channels
- **Session chaining**: sessions auto-generate next session when work remains
- **Autonomous task queue**: high/medium/low/background priority levels
- **Decision engine**: defined bounds for autonomous vs Jorge-required decisions
- **Time planner**: daily rhythm (active work, maintenance, quiet hours)
- **Token budget**: 500K daily, 20% reserved for Jorge interactions
- **Multi-modal input**: Telegram, email, calendar, git planned

### Autonomy progression (5 levels, 6 weeks)
- Level 1: Enhanced heartbeat (Week 1) — can start immediately
- Level 2: Event-driven triggers (Week 2)
- Level 3: Session chaining (Week 3)
- Level 4: Decision engine (Week 4)
- Level 5: Multi-modal operation (Week 5-6)

### PACK R3 updated
- Now uses autonomous session chaining model
- 24-48h wall clock (autonomous), 12-18h active work
- Only 2 Jorge intervention points (Session 2 decisions + final review)
- Sessions chain automatically between intervention points

### Pending Jorge decisions for autonomous operation
1. Can I install cron jobs and git hooks?
2. What communication channels? (Telegram, email, both)
3. Preferred interaction schedule?
4. Autonomy level? (what I decide alone vs notify)
5. Daily token budget?
